# 系统 / 编辑器文件
.DS_Store
.idea/

# 包管理器缓存
.pnpm-store/
.npm-cache/
node_modules/

# TypeScript 增量编译缓存
*.tsbuildinfo

# 构建产物
# dsh-change-summary 的 lib/ 为构建产物，不入仓（其余插件的 lib/*.js 已入仓以便离线加载）
dsh-change-summary/lib/